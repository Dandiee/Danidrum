<UserControl x:Class="Danidrum.UserControls.NoteHighwayControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Danidrum.UserControls"
             xmlns:services="clr-namespace:Danidrum.Services"
             xmlns:danidrum="clr-namespace:Danidrum"
             d:DataContext="{d:DesignInstance IsDesignTimeCreatable=False, Type=danidrum:MainWindowViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <local:MidLineConverter x:Key="MidLineConverter"/>

        <DataTemplate x:Key="LaneNameTemplate" DataType="{x:Type services:LaneContext}">
            <Border BorderBrush="{DynamicResource MaterialDesign.Brush.Primary}"
                    VerticalAlignment="Stretch"
                    HorizontalAlignment="Stretch"
                    BorderThickness="0 1 1 0"
                    Background="{DynamicResource MaterialDesign.Brush.Primary.Dark}"
                    >
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center" Margin="10 0"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           TextTrimming="CharacterEllipsis"
                           Style="{StaticResource MaterialDesignBody2TextBlock}"/>
                <!--<danidrum:DrumIcon Articulation="{Binding KitArticulation}" VerticalAlignment="Center" Margin="10 0 0 0"/>-->
            </Border>
        </DataTemplate>
    </UserControl.Resources>


    <Grid Background="{DynamicResource MaterialDesignToolTipBackground}">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid x:Name="LaneNamesGrid" />

        <Grid Grid.Column="1">

            <ScrollViewer x:Name="NoteCanvasScroller"
                          SizeChanged="NoteCanvasScroller_SizeChanged"
                          VerticalScrollBarVisibility="Hidden"
                          HorizontalScrollBarVisibility="Hidden"
                          CanContentScroll="False">
                <Grid Margin="{Binding ElementName=NoteCanvasScroller, Path=ActualWidth, Converter={StaticResource MidLineConverter}}">
                    <local:MeasuresControl Song="{Binding Song}"
                                           MeasureBrush="{DynamicResource MaterialDesign.Brush.Primary}" MeasureThickness="1.5"
                                           IsMeasureInfoVisible="True"
                                           SubdivisionBrush="{DynamicResource MaterialDesign.Brush.Primary}" SubdivisionThickness="0.3"
                                           PixelPerMs="{Binding PixelPerMs}"/>
                    <Grid x:Name="LanesGrid"  />
                </Grid>
            </ScrollViewer>

            <Border Width="1" Background="{DynamicResource MaterialDesign.Brush.Secondary.Light}" HorizontalAlignment="Center" VerticalAlignment="Stretch" IsHitTestVisible="False" />

        </Grid>
    </Grid>
</UserControl>
